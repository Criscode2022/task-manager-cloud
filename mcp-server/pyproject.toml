[project]
name = "task-manager-mcp"
version = "0.1.0"
description = "MCP server for Task Manager Cloud — search, CRUD tasks, create users"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "bcrypt>=4.3.0",
    "mcp[cli]>=1.28.1",
    "psycopg[binary]>=3.2.9",
    "psycopg-pool>=3.2.6",
    "python-dotenv>=1.2.2",
]
